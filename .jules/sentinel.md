## 2024-03-01 - Authorization Bypass in n8n Filters
**Vulnerability:** A filter node designed for authentication was using `$if` to compare the incoming `chat.id` with the same `chat.id` retrieved dynamically via `$('Telegram Trigger').item.json.message.chat.id`. This effectively resulted in `if (incoming.chat.id == incoming.chat.id)`, making the condition always true.
**Learning:** In visual no-code workflows, it is easy to accidentally link an authorization verification check back to the untrusted input itself, instead of comparing it to a known static secret or environment variable.
**Prevention:** Hardcode or strictly reference environment variables for secrets/IDs (`=YOUR_TELEGRAM_CHAT_ID`) instead of writing complex dynamic fallbacks in filter conditions.
## 2024-10-24 - Missing Webhook Authentication
**Vulnerability:** The n8n Webhook node (used for incoming messages, such as WhatsApp via Evolution API) was completely unauthenticated and accepted any POST request to its URL. This allows anyone with the URL to spoof messages or cause a Denial of Service (DoS) by spamming the endpoint.
**Learning:** In n8n, webhooks do not enforce authentication by default. Without authentication, attackers can easily bypass internal filter nodes by crafting malicious payloads that match the expected structure.
**Prevention:** Always enforce authentication (e.g., `headerAuth`) directly on the Webhook trigger node to ensure requests are from a trusted source, before any data enters the workflow.
## 2024-10-25 - Missing Authentication on Email Triggers
**Vulnerability:** The `Gmail Trigger` node was completely unauthenticated and accepted emails from any sender, directly passing them to the main AI agent flow. Since the agent has powerful tool access (Google Drive, databases, outbound email), an attacker could send a malicious email (e.g., prompt injection) to the monitored inbox and potentially take control of the agent's actions or extract data.
**Learning:** AI agent workflows triggered by external messaging sources (like Email, Telegram, or Webhooks) must enforce strict sender authorization. If the trigger node itself doesn't offer native filtering, an immediate downstream filter node is critical.
**Prevention:** Always add a Filter node immediately after email/messaging triggers to explicitly allow-list trusted senders (e.g., verifying `{{ $json.headers.from }}` contains an authorized address) before any AI processing or tool execution occurs.
## 2024-08-30 - Replace Hardcoded Placeholders in Sender Authorization Filters
**Vulnerability:** Sender authorization filters in n8n workflows used hardcoded dummy placeholders (e.g., `YOUR_TELEGRAM_CHAT_ID`, `YOUR_PHONE@s.whatsapp.net`) instead of proper environment variables, causing them to fail closed and break workflow functionality in production deployments, while masking a lack of genuine strict sender authorization setup.
**Learning:** Hardcoded dummy values in n8n nodes not only break production functionality, but they give a false sense of security during setup. An authorization filter must dynamically compare incoming sender IDs with explicitly defined environment variables (like `$env.AUTHORIZED_TELEGRAM_CHAT_ID`) to ensure authorized usage and functionality.
**Prevention:** Always use `={{ $env.VARIABLE_NAME }}` syntax for authentication/authorization checks in n8n visual workflows rather than deploying with literal placeholder strings.
## 2024-10-26 - Email Spoofing via Friendly Name in Authorization Filters
**Vulnerability:** The email sender authorization filter used a `contains` operation to check if the incoming `From` header contained the authorized email address (`$env.AUTHORIZED_EMAIL`). This could easily be bypassed by an attacker using a forged "Friendly Name" containing the authorized email (e.g., `"authorized@example.com" <hacker@evil.com>`), allowing malicious emails to pass the filter.
**Learning:** Using weak string operations like `contains` for authentication checks against complex fields like email `From` headers is highly insecure and leads to easy spoofing attacks.
**Prevention:** Always extract the exact, raw identifier (e.g., the raw email address) and compare it using a strict `equals` operation. In n8n, this means using a JS expression to parse the `From` header and enforcing exact equality.
## 2024-10-27 - Robust Email Parsing against RFC 5322 Address Spoofing
**Vulnerability:** The email sender authorization filter was parsing the sender address using naive string operations (`.split('<').pop().replace('>', '')`). An attacker could bypass the filter by supplying malformed RFC 5322 email headers (like trailing comments or nested elements), causing the parser to output corrupted strings, or by utilizing case-sensitivity discrepancies.
**Learning:** Naively parsing complex RFC 5322 email strings using basic delimiters is highly brittle and bypassable, potentially causing false-negative bypasses or unexpected crashes/errors. Case-sensitivity in simple string matching can also cause denial of service or auth bypass.
**Prevention:** Implement strict regex extraction that securely isolates the email address (or defaults to the bare address) and normalizes it to lowercase before comparison. Example: `={{ ($json.headers.from.match(/<([^>]+)>/)?.[1] || $json.headers.from).trim().toLowerCase() }}` and compare against lowercased variables.
## 2024-10-28 - Loose Type Validation in Authorization Filters
**Vulnerability:** Sender authorization filters (Telegram, WhatsApp, Gmail) used `loose` type validation in n8n's filter conditions (`"typeValidation": "loose"` and `"looseTypeValidation": true`). This allows type coercion during equality checks, potentially leading to unintended matches (e.g., if an incoming numeric ID is compared to an unset or improperly typed environment variable, or string-to-number coercions that strip leading zeros or characters).
**Learning:** In n8n, "loose" validation in filters uses `==` instead of `===`. When validating security boundaries (like checking incoming `chat.id` against `$env.AUTHORIZED_TELEGRAM_CHAT_ID`), strict type checking is required to prevent bypasses via type juggling or unexpected coercion behavior.
**Prevention:** Always set `"typeValidation": "strict"` and `"looseTypeValidation": false` in n8n Filter nodes that are used for authentication or authorization checks.
## 2024-10-29 - Missing Security Headers on Webhook Node
**Vulnerability:** The publicly exposed Webhook node in the n8n workflow was missing essential HTTP security headers (e.g., `X-Content-Type-Options`, `X-Frame-Options`, `Strict-Transport-Security`). This lack of defense-in-depth leaves the endpoint more vulnerable to common web-based attacks such as MIME sniffing, Clickjacking, and man-in-the-middle attacks when clients interact with it.
**Learning:** In n8n, exposing webhooks securely isn't just about authentication; it also involves setting proper HTTP response headers to protect against browser-based vulnerabilities. By default, n8n webhook nodes do not automatically include all recommended security headers.
**Prevention:** Always explicitly configure the `options.responseHeaders` property on publicly exposed Webhook nodes in n8n to include standard security headers (`X-Content-Type-Options: nosniff`, `X-Frame-Options: DENY`, `Strict-Transport-Security: max-age=31536000; includeSubDomains`) to enforce strict security policies for any interacting clients.
## 2024-10-30 - Incomplete Security Headers on Webhook Node
**Vulnerability:** The publicly exposed Webhook node in the n8n workflow had some HTTP security headers configured (like `X-Content-Type-Options` and `X-Frame-Options`) but was missing the crucial `Content-Security-Policy` (CSP) header. This incomplete defense-in-depth leaves the endpoint theoretically vulnerable to Cross-Site Scripting (XSS) if accessed directly in a browser environment that bypasses content sniffing mitigations.
**Learning:** While basic security headers are a great start, a strict `Content-Security-Policy` is a standard and powerful defense-in-depth mechanism for API and webhook endpoints.
**Prevention:** Always ensure a comprehensive set of security headers is defined on Webhook nodes, including `Content-Security-Policy: default-src 'none'` for JSON/API-only endpoints, to fully neutralize any execution contexts.
## 2024-11-01 - IDOR Vulnerability via Hardcoded Session Keys
**Vulnerability:** In n8n LLM agent workflows, multiple memory nodes (like `memoryPostgresChat` and `memoryBufferWindow`) were using hardcoded placeholder values (like `YOUR_USERNAME`) for their `sessionKey` parameter. This results in severe data leakage and an Insecure Direct Object Reference (IDOR) vulnerability, as all users of the workflow would share the same conversation context and be able to see each other's data and agent responses.
**Learning:** For multi-tenant AI agents, session management is critical. The session key must uniquely identify the individual user or conversation thread to prevent context cross-contamination.
**Prevention:** Always dynamically bind the `sessionKey` to a verified user identifier from the workflow execution context, using the normalized variable (e.g., `={{ $json.username }}`) across all memory nodes.
## 2024-11-02 - IDOR Vulnerability via AI-Controlled Identifiers in Tools
**Vulnerability:** In the n8n dataTable tools (`Initialization - Update Info` and `Update User and Heartbeat`), the `username` filter condition and column value were bound to an AI-controlled parameter (`$fromAI('username', ...)`). This resulted in an Insecure Direct Object Reference (IDOR) vulnerability, as an attacker could manipulate the AI input to overwrite or access the profile of a different user.
**Learning:** Security-critical identifiers like `username` or session keys must never be trusted from AI generation. AI models are susceptible to prompt injection and can be tricked into providing someone else's identifier.
**Prevention:** Always hardcode or dynamically bind security-critical identifiers to a verified context variable (e.g., `={{ $json.username }}`) in data modification tools to enforce authorization boundaries.

## 2026-09-11 - Clean Security Audit: n8nClaw Workflow Verified
**Vulnerability:** No critical vulnerabilities found. The entire `n8nClaw.json` workflow was audited, and all historic vulnerabilities have been successfully remediated.
**Learning:** All critical surfaces in `n8nClaw.json` were audited and verified secure:
- **Session Isolation:** All Memory nodes are dynamically bound to `={{ $json.username }}`.
- **Injection & Spoofing:** SQL queries are properly parameterized; email authorization utilizes strict lowercase regex extraction.
- **Transport & Boundary Security:** Webhook enforces `headerAuth`, `CSP default-src 'none'`, and standard security headers; authorization filters enforce strict type validation.
**Prevention:** Avoid speculative modifications (such as changing AI-controlled parameters in dataTable nodes) without explicit user-ownership schema, as this risks breaking production workflows and constitutes security theater.
